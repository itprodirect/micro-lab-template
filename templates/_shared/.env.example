# Application environment: dev, staging, production
APP_ENV=dev

# Logging level: trace, debug, info, warn, error
LOG_LEVEL=info

# Application name (used in structured log output)
APP_NAME=__REPO_NAME__
